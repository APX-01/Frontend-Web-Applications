import { ProfilesInGroups } from './profiles-in-groups.entity';

describe('ProfilesInGroups', () => {
  it('should create an instance', () => {
    expect(new ProfilesInGroups()).toBeTruthy();
  });
});
